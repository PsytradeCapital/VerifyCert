# Production environment variables for VerifyCert nd
# Disable CI mode to prevent ESLint warningsuild
CI=false

# Generate sourcemaps fo
GENERATE_SOURCEMAP=false

ning
BROWSER=none

# Optimize bundle sizeINLINE_RUNTse_CHUNK=falIME
