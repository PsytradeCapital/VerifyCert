t;eployContracts = dle.expor
}

modu;
    })xit(1);.e process     );
rror(errorole.e
      consr) => {atch((erro)
    .cit(0).ex=> process .then(()    
()ployContractde
  e) {= modulain ==ire.mqument
if (re Run deploy
// }
}
w error;
 thro    ssage);
ror(error.meonsole.er
    cailed:");nt fn❌ Deploymee.error("\consol  ) {
  (error } catch     
 e.address;
certificatn   retur
  );
    ssfully!"eted succet complloymeng("\n✅ Dep.loole  cons}
    
  ;
    hnology/")on.tecucet.polyg/fas:/g("http.losole  con");
    t MATIC:tes"\n💰 Get log(onsole.;
      crk amoy`)netwo.js --ify-amoypts/verridhat run scharg(`npx e.lo    consolact:");
  ontrify the cn📝 To verlog("\    console.) {
  amoy' 'rk ===argetNetwo if (t 
     );
 Url}`rerloer: ${exp explorblock🔍 View on e.log(`\n   consol
      
 ddress}`;cate.as/${certificom/addresn.gonscaumbai.poly: `https://m    ddress}`
  ertificate.address/${ccom/alygonscan./amoy.pottps:/
      ? `hamoy' = '==work argetNeterUrl = tor const expl;
    
   repeat(60))="."og(ole.l
    conson.hash}`);ransactideployTtificate.: ${cersactionlog(`📦 Tranle.onso  c);
  ess}`.addryerlor: ${depeployeole.log(`👤 Dcons
    inId})`);g.cha.confiD: ${network(Chain Ik} twor: ${targetNe Networksole.log(`🌐;
    con`)dress}ificate.ad{certAddress: $tract g(`📍 Connsole.lo    coeat(60));
ep("=".re.log  consol");
  SFUL!ENT SUCCES("🎉 DEPLOYMe.logonsol    cat(60));
=".repe+ "g("\n" le.loonso    c
;
s.json")sseact-addreto contr   ✅ Saved g("e.lo consoll, 2));
   Info, nulmentloyringify(depon', JSON.stses.js-addrestractconFileSync('./    fs.write
to root// Save   
       };
  an.com'
 .polygonsc/mumbai' : 'https:/.comonscan/amoy.polyg:/ 'https' ?rk === 'amoyetwoargetNkExplorer: t
      blocnfig.url,cork.Url: netwo rpc,
     alseerified: f),
      voString(ce.tiveGasPriceipt.effectGasPrice: re  effective(),
    toStringsed.gasUt. receip   gasUsed:mber,
   t.blockNuer: receip   blockNumbhash,
   ansaction.oyTricate.deplrtifsh: ceransactionHas,
      tdresad deployer.er: deploy   ing(),
  e().toISOStrt: new DatployedAde  nId,
    g.chaiork.confinetw   chainId: twork,
   rgetNeork: ta   netw",
   rtificate: "SimpleCectName  contradress,
    rtificate.adcectAddress: tra
      conntInfo = {ployme    const de");
    
ormation...yment infdeploving ("\n💾 Salogconsole.tion
    t informaymenlo dep Save   //
 `);
l: ${symbol}ract symboont(`   Cle.lognso   coame}`);
 e: ${ntract namg(`   Conloconsole.
    ymbol();.scertificateol = await nst symb co();
   cate.nameertifie = await cnst nam    co
zed}`);
ri{isAutho $ authorized:eployerg(`   Dlonsole.   codress);
 r.adyers(deploizedIssuethortificate.auerit crized = awaisAutho
    const    owner}`);
 t owner: ${acControg(`    console.l
   ();icate.ownertifcer= await onst owner  c
    
   .");ntract.. coployedting de🧪 Tesog("\n   console.llity
 functionast basic 
    // Teng()}`);
oStrigasUsed.t{receipt.s used: $log(`   Gaonsole.r}`);
    cckNumbeeceipt.blo block: ${rrmed in(`   Confi console.logs);
   nfirmation.wait(coctionTransa.deploytetificacerait  = aweceipt   const r 2;
 moy' ? 3 :== 'aetNetwork =ions = targst confirmat  conmations
  confir Wait for   //s}`);

  ate.addres${certificyed to: ract deploog(`✅ Contnsole.ld();
    codeployeificate.  await cert);
    
  ation..."ent confirmor deploym   Waiting fsole.log("con`);
    hash}nsaction.deployTraertificate.{chash: $n tio`   Transacog(e.l
    consoly();
    ate.deploiceCertifmpl= await Sicate fiertist c
    cone");ficatrti"SimpleCectFactory(tContraethers.geate = await CertificSimpleonst 
    
    c");...ctte contrartifica SimpleCeDeploying\n🔨 sole.log("y)
    conmo Aefault forract (de contaticCertifloy SimpleDep

    // ; MATIC`)(balance)}Ethers.formatrs.utilnce: ${etheog(`   Balansole.l  co();
  alanceyer.getB deploitawa= nst balance  
    coess}`);
   r.addr: ${deploye  Deployerog(` onsole.l;
    c`)ig.chainId}onf{network.chain ID: $ C.log(`  console
    `);etNetwork} ${targork:   Netwg(` console.lo");
   etails:yment Dplo("📋 Deoge.l
    consol
    ers();getSignait ethers.yer] = aw[deplo    const ccount
loyer adep/ Get    / try {
 
  }

 ;network`)lt Amoy d of defauk} insteaargetNetwor to ${tyinglo Deprning:⚠️  Walog(`   console.') {
 !== 'amoygetNetwork if (tar;
  
  | 'amoy'.name |networkork = targetNetwonst  cwork
 oy neto Amfault t/ De  /

));epeat(60.log("=".r  console;
k)...")fault networdeo Amoy (tract te conficatying Certilo🚀 Depe.log(" {
  consolyContract()eploction d
async fun
g();fi').conre('dotenv;
requi')require('fss = onst ft");
cre("hardha } = requi, network{ ethersconst 