;yDeploymentmotA= tesorts module.exp;
}


    })(1);ss.exit  proce   r);
 e.error(erro consol{
     error) => ch(( .cat
   ;
    })ess ? 0 : 1)ccexit(surocess. {
      puccess) =>hen((st()
    .tmoyDeploymen) {
  testAdule.main === mo
if (requireun the test
}

// R;
  }return false  sage);
  or.mes(errsole.error;
    conled:")❌ Test fai\n.error("olecons) {
    orch (err cat
  }n true;
    retur
    
;ology/")chn.polygon.te://faucetIC: httpsest MATt t Gelog("💰 console.
   Address}`);tractddress/${conn.com/agonsca://amoy.polytpst: htontracw cog(`🔗 Viele.lso con!");
   sts passed basic te("✅ Allle.log
    conso(50));at.repe.log("="    console);
"EDST COMPLETEPLOYMENT TEOY D("🎉 AMe.log
    consolt(50));".repea+ "=" .log("\n  console'}`);

  ' : 'FAIL 0 ? 'PASSceInMatic) >balan{parseFloat(e check: $anc  ✅ Bal.log(` olenscoTIC`);
    tic} MAnceInMa ${balae:(`   Balancconsole.logance);
    tEther(bals.formahers.util= etic InMatlanceonst ba);
    cce(alanloyer.getBdepait nce = awconst balace");
    ount Balan 6: AccTest"\n🔍 g(nsole.lo
    coe checkBalancTest 6: 

    // '}`);ILASS' : 'FA80002n ? 'P=== fo.chainId Ink: ${networkork checetw   ✅ Ng(`sole.lo con`);
   ckNumber}ock: ${bloent bl   Currsole.log(``);
    conchainId}Info.orkID: ${netw   Chain og(`le.l conso
   );BlockNumber(getider.ethers.prover = await blockNumb    const etwork();
tNger.hers.providet etawai = fo networkIn;
    constn")nformatiok Ietwor 5: Nn🔍 Testlog("\nsole.on
    cormatirk info: Netwo  // Test 5
  
); PASS`k: Supply chec(`   ✅.logconsole()}`);
    ngpply.toStrilSuy: ${totaplsup(`   Total nsole.log();
    copplytotalSurtificate.await ceSupply = const total    ");
upplyl STotat 4: 🔍 Tesg("\n  console.loy
  uppl sCheck totalst 4: Te

    // ;FAIL'}`) 'PASS' : 'ERT' ?mbol === 'VCsycate' && tifiyCert CerVerif === '{name check: $✅ Metadataole.log(`   ns}`);
    co ${symbol  Symbol:le.log(`   conso
   ${name}`);(`   Name: console.log);
   bol(icate.symifertl = await cst symbocon;
    icate.name()rtif= await cest name  con");
   ct Metadatast 3: Contran🔍 Telog("\  console.  symbol
d e ancontract namst 3: Check  Te;

    //}`)AIL'PASS' : 'Fized ? 'uthorheck: ${isAorization c   ✅ Auth(` console.logd}`);
   Authorized: ${isorize  Is authe.log(`     consoless);
dryer.adploers(deIssurizede.authotificatwait cerorized = aisAuthnst );
    cohorization"Issuer Aut2: est "\n🔍 Tole.log(ed
    consoriz is authyer if deplockhe// Test 2: C);

    '}` : 'FAILASS' ? 'PoLowerCase()s.tr.addresploye) === deCase(werowner.toLock: ${ner che Ow  ✅log(`  console.;
   ddress}`)yer.aeplocted: ${dog(`   Expeonsole.lr}`);
    c${owne  Owner: e.log(`   consoler();
  ficate.ownait certier = aw const ownner");
   Contract Ow1: est log("\n🔍 T  console.owner
  ck contract  1: Chest // Te}`);

   ressractAdds: ${cont addresractog(`📍 Contle.l

    consos);esdrh(contractAdcate.attaceCertifi Simplrtificate =   const cecate");
 leCertifiory("SimptFacts.getContracerawait ethcate = ifileCert Simp
    const24271F50";AEe1f9975982eEA1b54C721dA0C6c9D50x= "ress Addt contractt
    constrac conet

    // Gress}`);ddloyer.a{depount: $ with accTestingle.log(`👤 
    consors();.getSigne ethersr] = awaitployenst [deount
    coloyer acc  // Get dep  }

    `);
moyted: a Expeck.name}.k: ${networWrong networ new Error(`hrow    t
   {== 'amoy')ame ! (network.nifwork
    on Amoy net're rify we
    // Ve try {;

 at(50))".repe"=e.log( consol");
 oyment...Deplng Amoy 🧪 Testile.log("so con {
 loyment()testAmoyDepction nc fun;

asyig()v').confdotenquire('dhat");
reharire("requrk } = rs, netwoetheconst { 