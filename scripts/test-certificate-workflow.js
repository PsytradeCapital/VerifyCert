flow;ficateWorktestCertirts = expoule.

mod
}1);
    });xit(cess.e
      proerror);.error(nsole
      co {) =>catch((error
    .1);
    }) 0 : t.success ?resulss.exit(   proce
   esult) => {(r  .then(w()
  florktificateWo{
  testCere) = modulain ==require.mest
if (/ Run the t}

/ };
  }
message
    error.     error:: false,
 uccess{
      s return ssage);
   ror.mee.error(erconsol");
    test failed:w rkflote woertifica❌ C"\nor(rrconsole.e  
  or) {} catch (err   };

  rUrl
 tExplorerl: contraclorerU   exp
   icationUrl,erif: vnUrliorificat   veh,
   as tx.hactionHash:   transd,
   d: tokenI     tokenI true,
 s:cces    su
  {
    return 0));
    repeat(6.log("=".
    consoleorerUrl}`);contractExpl ${w on Amoy:e.log(`🌐 Vie  consol);
  tionUrl}`verification URL: ${🔗 Verificale.log(`  conso`);
  tokenId}e ID: ${ificatog(`📜 Certsole.l  con
  lly!");ed successfucompletlow steps All workfe.log("✅   consol  t(60));
repea".og("=e.lonsol   c");
  COMPLETEDRKFLOW TESTRTIFICATE WO"🎉 CEsole.log();
    cont(60)=".repea" + "g("\n.lo  console

  ted");genera links orerxpl✅ E("   onsole.log}`);
    corerUrlExplenok: ${t(`   Token.log console   ;
rerUrl}`)xExplosaction: ${tan  Tre.log(`     consol);
rl}`rerUctExplo${contra Contract: e.log(`  
    consol`;
    Id}}?a=${tokenractAddressntm/token/${coconscan.lygoy.po`https://amoUrl = ertokenExplort ns    cosh}`;
/${tx.ham/txan.cogonscmoy.polys://a = `httprUrlploreconst txExss}`;
    Addreract{conts/$m/addres.conscanoy.polygos://am `httporerUrl =ctExplntra co   const;
 Links")rer plorate Extep 7: Genelog("\n🔗 Sle. consor links
   orend expl a7: Network Step );

    //ul"sfon succes revocatitificate ✅ Cerog("  ole.l cons  
    
  }");
   onevocatialid after r be invcate should("Certifiror new Erowthr    {
  ke.isValid) taAfterRevoicateDartif cerRevoke ||Afte (isValid
    if
    sValid}`);Revoke.ieDataAfterattificerd flag: ${caliificate vCertlog(`   le.    conso;
e}`)AfterRevokValid${isevoke: d after rli  Is valog(` e.consol
    );
    kenIdcate(totCertifitificate.geawait certerRevoke = aAfeDattificateronst c    c;
te(tokenId)tificae.isValidCerificat= await certterRevoke  isValidAfconstcation
    revo // Verify 

   ber}`);blockNumpt.Recei{revoken block: $atio   Revoc(`onsole.log c;
   keTx.hash}`)${revoansaction: n trioatg(`   Revocsole.lo con
    
   wait(2);evokeTx.ait rt = awip revokeRece const;
   te(tokenId)tificaeCercate.revok certifi awaitTx =evoke    const r");
vocationtificate ReTest Cer❌ Step 6: og("\nole.l
    consonate revocatiest certific6: T // Step 
   }
    }
;
      ssage}`)r.merErro${transfet reason: iffereniled for dransfer fag(`   ⚠️ Tsole.lo        cone {
      } elserable");
n-transfare noificates ocked - certtly blsfer correc✅ Tranle.log("    conso {
       ))able"nsferNon-trancludes(".message.iorransferErrf (t i
     {) ferErrorranscatch (t  } e");
  nsferabl-tra are nontestifica - cerailedhould have fsfer sor("Tran Err   throw new  Id);
 ress, tokendeployer.addwner, (tokenOtransferFromertificate.ait c  awble
    transferan-s are noateertifice ccausfail bed houl/ This s   /ry {
     t  ");
Restrictioner  Test Transf 5:n🚫 Stepg("\e.losol  conn
   restrictio transfercatet certifiTesp 5:  Ste//   
  }
age}`);
   rError.messd: ${qon faile generati⚠️ QR Code.log(`   ole     cons {
 or)catch (qrErr
    } sfully");succesed e generatCodg("   ✅ QR   console.lo);
    ters`rac} chaRL.lengthaUqrCodeDaterated: ${genQR Code e.log(`   sol     connUrl}`);
 verificatiotion URL: ${   Verificaog(`e.lonsol     c 
       });
 
           }F'
 t: '#FFFFF  ligh      000000',
  '#    dark:   r: {
    olo  c      
rgin: 2,    ma
    dth: 256,        wi {
rl,erificationUURL(vtatoDait QRCode.RL = awarCodeDataUnst q
      co try {   
    
dress}`;ontractAdt=${contrac{tokenId}&ckenId=$/verify?toifycert.apphttps://ver = `ionUrlverificat
    const e");QR Cod Generate ep 4:"\n📱 St(sole.log
    conerificationor v QR Code f: Generate  // Step 4

  rified");ate data ve✅ Certificg("   .loconsole  }

    ");
  smatch miwnern or("Tokero Er  throw new)) {
    se(ss.toLowerCaentAddreecipi() !== rrCaseer.toLowewnif (tokenO}
    ");
    lid not vate isCertificarror(" throw new E    id) {
 !isVal   if ();
    }
  mismatch"metitution naInsr("rro E newow{
      thrnName) ioinstitut !== utionNametita.insficateDatcerti   if (  }
 tch");
  name misma"Course  Error(new   throw 
   {me) rseNa !== coumeeNaoursateData.cfic if (certi  
     }smatch");
miname ient r("Reciprronew E   throw e) {
   ientNam recipntName !==cipieta.reteDaicatif   if (cerrity
  data integerify
    // V
`);{tokenOwner} $wner: oenlog(`   Toksole.
    conisValid}`); ${  Is valid:ole.log(` ns    cor}`);
.issuetificateDatauer: ${cerIssole.log(`      cons);
 ng()}`triISOS) * 1000).tota.issueDatecateDacertifiate(Number(w D{neue date: $og(`   Iss.l    console
Name}`);nstitution.itificateDataon: ${cer  Instituti.log(`    console);
 rseName}`ouData.ctificate{cer: $rse nameou`   Clog(le.onsoe}`);
    crecipientNamta.ertificateDae: ${cpient namlog(`   Reci  console.enId);

  ownerOf(tokte.ertificawait cwner = aconst tokenO  nId);
  e(tokecatCertifilidisVatificate.ait ceralid = aw   const isVd);
 te(tokenICertificaeticate.gcertifait Data = awtificate   const cer
 ata");e DCertificaty Veriftep 3: "\n🔍 Sg( console.lo   a
aticate d certifVerify/ Step 3:   /

  ");y successfullssuedate iCertific"   ✅ g( console.lo   okenId}`);
ken ID: ${ttificate to  Cere.log(` 
    consoltring();wSupply.toS neokenId =t t
    consly();Suppotale.tat certificitwaupply = anst newSn ID
    cow tokeGet the ne    // 
g()}`);
ined.toStrUseipt.gasd: ${recas use   Gle.log(`
    consoNumber}`);lockt.beip: ${recumberBlock n`   log(le.
    conso2);t(aix.wwait t aceipt =st re  conhash}`);
   hash: ${tx.actionransle.log(`   T

    conso
    );   }fer
   n // 20% buf / 100 * 120nasEstimateLimit: gas       g
      {
 ame,tutionN      instiurseName,
,
      coNameecipient
      r,tAddressipien
      recicate(ssueCertife.itificatertx = await c
    const tecertificae 
    // Issuing()}`);
.toStrEstimate{gastimate: $`   Gas esconsole.log(;
    
    )onNametutinsti,
      i courseName   entName,
     recipi,
   ddresspientA  reci(
    timateGase.esueCertificatisste.t certifica= awaiEstimate gasonst gas
    cEstimate //   

  me}`);utionNa: ${institstitution`   Inlog(le.
    consoe}`);rseNam: ${courseCou.log(`   nsole
    coress})`);ecipientAddme} (${rpientNat: ${reciRecipienog(`   e.l consol

   y"; AcademVerifyCertonName = "tist institu
    conntals";nt Fundamen DevelopmeBlockchai = "seNamet cour   consnson";
  "Alice JohtName =ecipienst r";
    con96C4b4d4d425a3b8D4C9db4C05329663"0x742d35CcntAddress = pieeci
    const r");ificatee Test Cert 2: IssuStep.log("\n📜    console
 ateest certificsue a t Step 2: Is  //");

  erified setup vontractg("   ✅ Ce.lo consol
   );
    }es"e certificat issued tothorizr is not auoyer("Deplnew Erro    throw rized) {
  isAutho
    if (!
    );ng()}`triy.toSpltialSup${iniy: ppl su Initial.log(`  leconso`);
    ized}horsAutd: ${ithorizeloyer aug(`   Depole.lo
    conser}`);owner: ${ownContract og(`   e.l
    consol
    ly();.totalSuppertificatewait c alSupply =initia
    const address);rs(deployer.suethorizedIsrtificate.aud = await ceuthorizeisA   const 
 ate.owner();icifwait certt owner = ans    coSetup");
ntract fy Coep 1: Veri St.log("\n📋 consolesetup
   ract y conttep 1: Verif
    // S})`);
ig.chainId.conf${network ID: ame} (Chaink.nor: ${netwg(`🌐 Networkloole.;
    consress}`)actAddss: ${contrt addreontrac📍 Cog(`onsole.l
    cdress);
ntractAd.attach(cocatepleCertifi= Simtificate ernst c;
    cocate")rtifiimpleCery("SractFactohers.getCont etaite = awleCertificatonst Simp
    c271F50";f924e1A1b975982eAEA0CE1d4C726c9D55= "0xress ontractAddonst c cract
    Get cont  //ss}`);

  dreeployer.adnt: ${dth accou wig(`👤 Testing console.lo
   igners();.getSawait ethersployer] =   const [deaccount
  er / Get deploy  }

    /`);
  : amoy. Expectedwork.name}et${nk: networor(`Wrong w new Err   thro {
   amoy')e !== 'amrk.n(netwo if 
   moy networkre on Afy we'Veri  //   

  try {));
t(60.repeag("=".lonsole
  co");...moy Aorkflow onicate Wete Certifg Compl🧪 Testinog(" console.l) {
 eWorkflow(ificat testCertunctionc fsynfig();

aenv').con'dot
require(');e('qrcodede = requir
const QRCoat");ire("hardh= requk } wornethers,  { etconst